Place the production-signed ViGEmBus installer here as:

ViGEmBusSetup.exe

You can fetch it with:

powershell -ExecutionPolicy Bypass -File ..\download_vigembus.ps1

The app checks this path when ViGEmBus is missing and the user chooses "Install ViGEmBus".
